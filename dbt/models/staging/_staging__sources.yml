version: 2

sources:
  - name: raw_maju
    database: olist-platform
    schema: raw_maju
    tables:
      - name: raw_customers
        columns:
          - name: id
            tests: [unique, not_null]
      - name: raw_sales
        columns:
          - name: vin
            tests: [unique, not_null]
      - name: raw_after_sales
        columns:
          - name: service_ticket
            tests: [unique, not_null]
      - name: raw_customer_addresses